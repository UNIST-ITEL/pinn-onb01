# PINN-ONB01 — Phase 1 Pool Boiling Project Dependencies
# Install with: pip install -r requirements.txt

# ── PDF / text extraction (Phase 1: 서베이) ─────────────────
pdfplumber>=0.11
pypdf>=4.0

# ── Data handling (Phase 2: 데이터 전처리) ───────────────────
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyyaml>=6.0
scikit-learn>=1.4
CoolProp>=6.6

# ── PINN modeling (Phase 3: 모델링) ─────────────────────────
torch>=2.1
deepxde>=1.11

# ── Experiment tracking + HPO (Phase 3) ─────────────────────
mlflow>=2.10
optuna>=3.5

# ── Visualization (Phase 4: 분석) ───────────────────────────
matplotlib>=3.8
seaborn>=0.13

# ── Comparison baselines (Phase 4) ──────────────────────────
xgboost>=2.0

# ── Notebook (선택) ─────────────────────────────────────────
jupyter>=1.0
ipywidgets>=8.0

# ── Dev / testing ───────────────────────────────────────────
pytest>=7.4
